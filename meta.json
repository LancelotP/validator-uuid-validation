{
    "directions": "./directions.md",
    "language": "javascript",
    "framework": "mocha",
    "version": "",
    "editables": [
        "./sources/uuid-validator.js"
    ],
    "commands": {
        "install": [
            "npm install",
            "npm test"
        ],
        "run": "npm test"

    },
    "steps": [
        {
            "title": "should invoke validator.isUUID"
        },
        {
            "title": "should invoke isUUID with the proper arguments"
        },
        {
            "title": "validateUUID() should return true if given value is an UUID"
        },
        {
            "title": "validateUUID() should return false if given value is not an UUID"
        }
    ]
}
